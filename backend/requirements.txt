# SkillSync backend requirements
# Install: pip install -r requirements.txt

# ── API framework ─────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9        # for PDF file uploads

# ── ML / NLP  (must match notebook) ──────────────────────────────────────────
torch==2.3.1                   # CPU wheel is fine; GPU if available
transformers==4.44.2           # BertTokenizer + BertModel
faiss-cpu==1.7.4               # vector search
numpy==1.26.4

# ── PDF parsing ───────────────────────────────────────────────────────────────
pdfplumber==0.11.4             # used by /recommend/pdf

# ── Validation / serialisation ────────────────────────────────────────────────
pydantic==2.9.2

# ── Dev ───────────────────────────────────────────────────────────────────────
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0
